name: Set dashboard header to current date

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/set-current-dashboard-date.yml

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Replace static header date
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

